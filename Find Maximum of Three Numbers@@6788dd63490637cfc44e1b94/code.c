#include <stdio.h>

int main() {
    int x,y,z,max;
    scanf("%d%d%d",&x,&y,&z);
    max=a;
    if(b>max){
        max=b;
    }
    else{
        max=c;
    }
    printf("%d",max);
    return 0;
}