#include <stdio.h>

int main() {
    char Name[100], Age[100], Hobby[100];
    scanf("%s %s %s",&Name,&Age,&Hobby);
    printf("Name: %s\n Age: %s\n Hobby: %s", Name, Age, Hobby);
    return 0;
}