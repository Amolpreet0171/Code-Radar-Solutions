#include <stdio.h>

int main() {
    float x,y,z;
    scanf("%f %f %f",&x,&y,&z);
    if(a+b>c && a+c>b && b+c>a){
        printf("Valid");
    }
    else{
        printf("Invalid");
    }
    return 0;
}